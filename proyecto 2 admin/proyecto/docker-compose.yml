version: "3.9"

services:

#Bases de datos : 
#------------------------------------------#

  # ==========================
  # 🗄️ Autenticacion
  # ==========================
  auth-db:
    image: postgres:16.4
    container_name: auth_db
    environment:
      POSTGRES_USER: ${AUTH_DB_USER}
      POSTGRES_PASSWORD: ${AUTH_DB_PASSWORD}
      POSTGRES_DB: ${AUTH_DB_NAME}
    ports:
      - "${AUTH_DB_PORT}:5432"
    volumes:
      - auth_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${AUTH_DB_USER} -d ${AUTH_DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
    restart: unless-stopped
    networks:
      database_net:

  # ==========================
  # 🗄️ Base de datos de Reservas
  # ==========================
  reservations-db:
    image: postgres:16.4
    container_name: reservations_db
    environment:
      POSTGRES_USER: ${RESERVATIONS_DB_USER}
      POSTGRES_PASSWORD: ${RESERVATIONS_DB_PASSWORD}
      POSTGRES_DB: ${RESERVATIONS_DB_NAME}
    ports:
      - "${RESERVATIONS_DB_PORT}:5432"
    volumes:
      - reservations_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${RESERVATIONS_DB_USER} -d ${RESERVATIONS_DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
    restart: unless-stopped
    networks:
      database_net:

  # ==========================
  # 🗄️ Base de datos de Documentos
  # ==========================
  documents-db:
    image: postgres:16.4
    container_name: documents_db
    environment:
      POSTGRES_USER: ${DOCUMENTS_DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DOCUMENTS_DB_PASSWORD:-password}
      POSTGRES_DB: ${DOCUMENTS_DB_NAME:-documents}
    ports:
      - "${DOCUMENTS_DB_PORT:-5434}:5432"
    volumes:
      - documents_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DOCUMENTS_DB_USER:-postgres} -d ${DOCUMENTS_DB_NAME:-documents}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
    restart: unless-stopped
    networks:
      database_net:

  # ==========================
  # 📦 MinIO - Almacenamiento de archivos
  # ==========================
  minio:
    image: minio/minio:latest
    container_name: minio_storage
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY:-minioadmin}
    ports:
      - "${MINIO_PORT:-9000}:9000"
      - "${MINIO_CONSOLE_PORT:-9001}:9001"
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3
    restart: unless-stopped
    networks:
      storage_net:

  # ==========================
  # 🔴 Redis - Sistema de Cola
  # ==========================
  redis:
    image: redis:7-alpine
    container_name: redis_queue
    command: redis-server --appendonly yes
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      backend_net:

  # ==========================
  # ⚙️ Servicio de Autenticación (FastAPI)
  # ==========================
  auth-service:
    build:
      context: ./services/auth-service
      dockerfile: Dockerfile
    container_name: auth_service
    env_file:
      - .env
    environment:
      DATABASE_URL: "postgresql+psycopg://${AUTH_DB_USER}:${AUTH_DB_PASSWORD}@auth-db:5432/${AUTH_DB_NAME}"
    ports:
      - "${AUTH_SERVICE_PORT}:8000"
    depends_on:
      auth-db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped
    networks:
      backend_net:
      database_net:

  # ==========================
  # ⚙️ Servicio de Reservas (FastAPI)
  # ==========================
  reservations-service:
    build:
      context: ./services/reservations-service
      dockerfile: Dockerfile
    container_name: reservations_service
    env_file:
      - .env
    environment:
      DATABASE_URL: "postgresql+psycopg://${RESERVATIONS_DB_USER}:${RESERVATIONS_DB_PASSWORD}@reservations-db:5432/${RESERVATIONS_DB_NAME}"
    ports:
      - "${RESERVATIONS_SERVICE_PORT}:8000"
    depends_on:
      reservations-db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped
    networks:
      backend_net:
      database_net:

  # ==========================
  # ⚙️ Servicio de Documentos (FastAPI)
  # ==========================
  documents-service:
    build:
      context: ./services/documents-services
      dockerfile: Dockerfile
    container_name: documents_service
    env_file:
      - .env
    environment:
      DATABASE_URL: "postgresql+psycopg://${DOCUMENTS_DB_USER:-postgres}:${DOCUMENTS_DB_PASSWORD:-password}@documents-db:5432/${DOCUMENTS_DB_NAME:-documents}"
      MINIO_ENDPOINT: "minio:9000"
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:-minioadmin}
      MINIO_SECURE: "false"
      MINIO_BUCKET: "documents"
      AUTH_SERVICE_URL: "http://auth-service:8000"
      SECRET_KEY: "un-secreto-muy-fuerte-y-largo"
      ALGORITHM: "HS256"
    ports:
      - "${DOCUMENTS_SERVICE_PORT:-8003}:8003"
    depends_on:
      documents-db:
        condition: service_healthy
      minio:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8003/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped
    networks:
      backend_net:
      database_net:
      storage_net:

  # ==========================
  # ⚙️ Servicio de Notificaciones (FastAPI + Celery)
  # ==========================
  notifications-service:
    build:
      context: ./services/notificacion-service
      dockerfile: Dockerfile
    container_name: notifications_service
    env_file:
      - .env
    environment:
      SMTP_HOST: ${SMTP_HOST:-smtp.gmail.com}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_USER: ${SMTP_USERNAME:-}
      SMTP_PASSWORD: ${SMTP_PASSWORD:-}
      SMTP_FROM_EMAIL: ${SMTP_FROM_EMAIL:-noreply@sistema.com}
      SMTP_FROM_NAME: ${SMTP_FROM_NAME:-Sistema de Reservas}
      SMTP_TLS: ${SMTP_TLS:-true}
      REDIS_HOST: "redis"
      REDIS_PORT: "6379"
      CELERY_BROKER_URL: "redis://redis:6379/0"
      CELERY_RESULT_BACKEND: "redis://redis:6379/0"
      AUTH_SERVICE_URL: "http://auth-service:8000"
      RESERVATIONS_SERVICE_URL: "http://reservations-service:8000"
      DOCUMENTS_SERVICE_URL: "http://documents-service:8003"
      SECRET_KEY: "un-secreto-muy-fuerte-y-largo"
      ALGORITHM: "HS256"
    ports:
      - "${NOTIFICATIONS_SERVICE_PORT:-8004}:8004"
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8004/health')"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped
    networks:
      backend_net:

  # ==========================
  # ⚙️ Celery Worker (Procesamiento de Cola)
  # ==========================
  celery-worker:
    build:
      context: ./services/notificacion-service
      dockerfile: Dockerfile
    container_name: celery_worker
    command: celery -A celery_config worker --loglevel=info --concurrency=2
    env_file:
      - .env
    environment:
      SMTP_HOST: ${SMTP_HOST:-smtp.gmail.com}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_USER: ${SMTP_USERNAME:-}
      SMTP_PASSWORD: ${SMTP_PASSWORD:-}
      SMTP_FROM_EMAIL: ${SMTP_FROM_EMAIL:-noreply@sistema.com}
      SMTP_FROM_NAME: ${SMTP_FROM_NAME:-Sistema de Reservas}
      SMTP_TLS: ${SMTP_TLS:-true}
      REDIS_HOST: "redis"
      REDIS_PORT: "6379"
      CELERY_BROKER_URL: "redis://redis:6379/0"
      CELERY_RESULT_BACKEND: "redis://redis:6379/0"
    depends_on:
      redis:
        condition: service_healthy
      notifications-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "celery", "-A", "celery_config", "inspect", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    restart: unless-stopped
    networks:
      backend_net:

  # ==========================
  # 💻 Frontend (Nginx)
  # ==========================
  frontend:
    build:
      context: ./services/frontend
      dockerfile: Dockerfile
    container_name: proyecto_frontend
    ports:
      - "${FRONTEND_PORT}:80"
    restart: unless-stopped
    networks:
      frontend_net:

  # ==========================
  # 🌐 API Gateway (Nginx)
  # ==========================
  gateway:
    build:
      context: ./services/api-gateway
    container_name: proyecto_gateway
    ports:
      - "${GATEWAY_PORT}:80"
    depends_on:
      - frontend
      - auth-service
      - reservations-service
      - documents-service
      - notifications-service
    restart: unless-stopped
    networks:
      - frontend_net
      - backend_net

# ==========================
# Volúmenes persistentes
# ==========================
volumes:
  auth_db_data:
  reservations_db_data:
  documents_db_data:
  minio_data:
  redis_data:

# ==========================
# Redes
# ==========================
networks:
  frontend_net:
    driver: bridge
  backend_net:
    driver: bridge
  database_net:
    driver: bridge
  storage_net:
    driver: bridge